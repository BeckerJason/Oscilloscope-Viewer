# Oscilloscope Viewer

Downloads an SVG from the internet and converts it into XY output for an oscilloscope.

## What it does

* Fetches an SVG from a URL
* Processes the SVG paths
* Converts the paths into X/Y coordinates
* Outputs the coordinates for display on an oscilloscope in XY mode
